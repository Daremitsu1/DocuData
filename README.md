# DocuData
Repository of full scaled application to pull document data - index them according to FIPs, classify them into required document buckets, extract necessary entities and transform entities as per organization needs.
